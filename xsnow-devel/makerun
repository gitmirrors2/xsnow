#!/bin/bash
ulimit -c unlimited
rm -f core
reset
if false; then
   export CC=gcc-10 CXX=g++-10
   export CFLAGS='-Wextra -Wint-conversion -Wpedantic -Wall -O0 -g'
else
   export CC=g++-10 CXX=g++-10
   export CFLAGS='-Wextra -Wzero-as-null-pointer-constant -Wpedantic -Wall -O0 -g' 
fi
if [ ! -e Makefile ] ; then
   ./configure
fi
make   && src/xsnow --g-fatal-warnings
#make -j  && GDK_SYNCHRONIZE=1 GTK_DEBUG=interactive src/xsnow #--g-fatal-warnings
#make -j  && GTK_DEBUG=interactive src/xsnow --g-fatal-warnings

